"""
taxlib - the shared toolbox for the Hostlyft tax tracker.

Files in here are *libraries*: they hold reusable pieces (settings, currency
conversion, categorisation rules, notifications) but you never run them
directly to do a job.

The things you actually run live in  scripts/  instead.
"""
