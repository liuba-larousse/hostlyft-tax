# Settings for pytest, the tool that runs the automatic checks.
[pytest]
testpaths = tests
python_files = test_*.py
# -q keeps the output short; run_tests.command overrides it with -v for detail.
addopts = -q
